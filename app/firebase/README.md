This directory holds Firebase client initialization used across the App Router.

- Config is read from environment variables prefixed with `NEXT_PUBLIC_`.
- To use emulators locally, set `NEXT_PUBLIC_USE_EMULATORS=true` in `.env.local` and run `firebase emulators:start`.

