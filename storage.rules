rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // TODO: Replace with planned rules. Placeholder denies by default.
    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}

